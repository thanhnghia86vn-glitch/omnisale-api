# Web framework / API
fastapi==0.115.0
uvicorn[standard]==0.30.6

# HTTP client để gọi ra ngoài (gọi AI model, search, v.v.)
httpx==0.27.2
requests==2.32.3

# Xử lý dữ liệu
pydantic==2.9.2
python-dotenv==1.0.1

# AI / ML (tuỳ chọn, giữ tối thiểu để tránh quá nặng)
numpy==2.1.1

# Logging & tiện ích
rich==13.8.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0